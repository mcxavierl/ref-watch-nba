# Copy to .env.local and fill in secrets (never commit .env.local).
ODDS_API_KEY=

# API v1 (Data-as-a-Service)
# Dev keys JSON — non-production only; production uses D1 subscriptions table.
# API_V1_SUBSCRIPTIONS_JSON=[{"key":"rw_dev_free","tier":"FREE"},{"key":"rw_dev_pro","tier":"DEVELOPER"}]
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
# API_V1_RATE_LIMIT_DISABLED=1
